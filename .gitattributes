# Shell scripts and container files must keep LF endings — CRLF breaks the
# shebang inside Linux containers ("/bin/sh^M: bad interpreter").
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf
*.py text eol=lf
